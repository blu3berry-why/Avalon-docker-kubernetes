name: avalon-ingress

services:
  email_service:
    name: avalon-email-handler
    port: 80
    prefix: /playground
  gamelogic_service:
    name: gamelogic
    port: 80
    prefix: /

dependencies:
  gamelogic:
    enabled: true
  email:
    enabled: true
  traefik:
    enabled: true